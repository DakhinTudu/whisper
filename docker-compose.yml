# Local production-like run: docker compose up --build
services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      APP_CORS_ALLOWED_ORIGIN_PATTERNS: "http://localhost:*,http://127.0.0.1:*,https://*.netlify.app"

# Copy to .env.local for local dev (optional; Vite proxy works without this).
# For Netlify: set VITE_API_URL in Site settings → Environment variables (build-time).
#
# Your Render backend URL — NO trailing slash
# VITE_API_URL=https://secret-api.onrender.com
